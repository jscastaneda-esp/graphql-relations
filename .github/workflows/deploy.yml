name: Deploy manually

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Image version'
        required: true

jobs:
  deploy-to-cluster:
    name: deploy to cluster
    runs-on: ubuntu-latest
    steps:
    # - name: Install doctl
    #   uses: digitalocean/action-doctl@v2
    #   with:
    #     token: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}
    # - name: Log in to DO Container Registry
    #   run: doctl registry login --expiry-seconds 600
    - name: Update deployment file
      run: sed -i 's|<IMAGE>|registry.digitalocean.com/graphql-relations-test/graphql-relations:${{ github.event.inputs.version }}|' ${{ github.workspace }}config/deployment.yml
    # - name: Save DigitalOcean kubeconfig with short-lived credentials
    #   run: doctl kubernetes cluster kubeconfig save --expiry-seconds 600 graphql-relations
    # - name: Deploy to DigitalOcean Kubernetes
    #   run: kubectl apply -f config/deployment.yml
    # - name: Verify deployment
    #   run: kubectl rollout status deployment/graphql-relations
    